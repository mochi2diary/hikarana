## 概要

プロジェクト名 "hikarana"

未就学児(4～6歳想定)向けに，ある言葉を想起させる絵に対して，言葉を ひらがな または カタカナ でマス目に1文字ずつ書くスタイルの，ひらがな／カタカナの練習問題を作成する。

## システム概要

- 問題は1ページ分ずつ個別に作成する。
- 必要な絵(画像ファイル)の作成は ChatGPT または Gemini を使用して予め行っておく。
- 問題定義言語のmarkdownファイルからPDFを作成するプログラムは，兄弟プロジェクトの arithprac 同様に Ruby で作成し，Typst で PDF に生成する。
- 重要な注意点として，Typst は現バージョン(0.15系)でも縦書きに対応していない。
    - 本プロジェクトでのレンダリングの複雑さは一般的な組版と比べて限定的と考え，1文字ずつレンダリングするような Typst コードを出力する。
        - 地の文の字間はベタ組み（1em送り）とする。
        - 地の文で半角の英数字は使用不可とする(プログラムは警告を出して当該文字を無視する)。全角の英数字は使用可とする。
    - フォントは vert/vrt2 機能を適用して縦書き用の字形を使用する。
- 本文のフォントは Morisawa Fonts のフリープラン(アカウント作成済)の UDデジタル教科書体 ProN を使用する。
    - 当該フォントがインストールされた Windows 機で実行する。
    - デフォルトがJIS2004字体なので，教科書体として使用するためJIS90字体を指定して使用する。
- タイトルやセクションタイトルなど一部のフォントは BIZ UDゴシック (GitHub版 "BIZ UDGothic")を使用する。
    - タイトル と セクションタイトル はBoldのウェイトを使用する。
- これらの2フォントは vert/vrt2 機能を適用可能であり，縦書きフォントとして使用できる。
- フォントは事前に Typst で使用できるように設定を完了させておく。
- A4をlandscape形状で使用し，日本語の縦書きで使用する。

## ディレクトリ構成

- 画像は img/ ディレクトリに格納する。画像のファイル名指定は，img/ ディレクトリからの相対パスで行う。
- 問題記述は md/ ディレクトリに格納する。1ページ分の問題に対して1ファイルとする。
    - ファイル名は "nnnn.md" または "nnnn_description.md" 形式とする。
    - md/ ディレクトリにある上記ファイル名形式に合致する全ての .md ファイルを対象とする。
    - PDF 中では，ファイル名のASCII順にページが並ぶ。
- 最終的に生成した PDF は，out/ ディレクトリに格納する。
    - PDF は，全問題を1つのファイルにまとめて生成する。
- tmp/ には中間生成の Typst ファイルなどを入れて良い。

## 環境

- Windows 11 上の WSL2 環境を使用する。
- Ruby は 4.0 系が rbenv 管理でインストールされている。
- Typst は 0.15 系が cargo 管理でインストールされている。

## 印刷レイアウト

### マージン

- 上下左右ともに 10mm とする(プリンタの印刷限界)。

### タイトル

- ページ最右 上寄せ で BIZ UDゴシック の18ポイントを使用して記載する。
- 文字列のデフォルトは "ひらカナマスター" とする。縦書き。

### 問題種別番号

- ページ右下に横書きで，問題種別番号(後述) を記載する。
- 問題種別番号は算用数字4桁である。
- BIZ UDゴシック の 8ポイント を使用して記載する。

### 問題説明エリア

- タイトルの下15mmから下を問題説明エリアとする。縦書きである。
- BIZ UDゴシック の12ポイントを使用して記載する。
- 問題説明文 のデフォルトは "え と あうように わくのなかに じをかいてください。" とする。
- 問題説明文の折り返し等は考慮しない（問題作成者の責任とする）。

### 内容エリア

- 問題説明エリアから15mmあけて，内容エリアとなる。縦書きである。各行の開始位置は，上マージンの端とする。
- 内容エリアの文字は，セクションタイトルのみ BIZ UDゴシックを使用，他は全て UDデジタル教科書体 ProN を使用する(JIS90字体を指定)。
- 内容エリアには，セクションタイトル，通常の文字，記入欄，画像 を配置する。
    - セクションタイトル: 切れ目を示すための見出し。14ポイントを使用する。
    - 通常の文字: 地の文(読ませる)。12ポイント。
    - 記入欄: 1コマあたり横29.5mm × 縦29.5mmの正方形のマス目(枠線を含む外寸)。
        - この正方形のマス目を，拗音や記号を記載する範囲を示すために，縦横2つに分割(計4つの区画に分割)する。コマの枠線(境界線)は実線かつ黒で幅0.5mm，分割線は点線かつ灰色で幅0.5mmとする。
        - 記入欄が続く場合(複数文字の場合)はくっつける(続く記入欄は29.5mm×29.5mmマス目の境界線を共有する)。
            - 記入欄が1つ単独の場合，縦29.5mmとなる。
            - 記入欄が複数連続する場合，記入欄n個に対して縦(29.5*n-0.5*(n-1))mmとなる。
    - 画像: 正方形のエリアで 横30mm × 縦30mm の大きさとし，枠はない。指定された画像を縮小して表示する。
    - 通常の文字，記入欄，画像 は同一行に配置できる。
        - 通常の文字の左右中央，記入欄の左右中央，画像の左右中央が揃うように配置する。この左右中央の計算に，ふりがなは含めない。
        - 通常の文字と記入欄の縦間隔は 12pt 隙間を空ける。
        - 通常の文字と画像の縦間隔は 12pt 隙間を空ける。
        - 画像と画像の縦間隔は隙間を空けない。
        - 画像と記入欄の縦間隔は隙間を空けない。
        - 「他の行との隙間」は，その行の最も外側にある要素の外側端から測る。通常の文字しかない場合は通常の文字，記入欄や画像が混在する場合は，その外側から測る。ふりがながある場合は，ふりがなを含めた最も外側の要素の外側端から測る。
    - セクションタイトルと次の行の行間は 18pt の隙間，前の行との行間(内容エリアで前の行があれば)は，15mm の隙間とする。
        - セクションタイトルが仮に2行連続した場合も，18pt の隙間とする。
    - 通常の文字しかない行どうしの行間は，18pt の隙間とする。
    - 通常の文字しかない行と記入欄や画像がある行の間は，18pt の隙間とする。
    - 記入欄や画像がある行どうしの間は，18pt の隙間とする。
- 記入欄には，ふりがなを打てるようにする。
    - ふりがな指定がある場合は，記入欄それぞれのコマの右側に，9pt でふりがなを記載する。

### こっそり解答エリア

- ページ左下に縦書き(ただし文字も行の向きも180度反転で)解答を記載するエリア。
    - 解答なので，読みづらいように反対向きで記載。
    - このペーパーの主題は解答を考えることではなく字を書く練習をすることなので，解答はこの程度の隠し方で良い。
- 行の開始位置は，上マージンの端とする。行も文字も上下反転しているので，こっそり解答エリアの左下端が，マージンの左下端となる。
- こっそり解答エリアの文字も，全て UDデジタル教科書体 ProN を使用する(JIS90字体を指定)。サイズは 10pt とする。
- こっそり解答エリアと内容エリアが重ならないようにする責任は，問題作成者にある。

### 行はみ出し時の処理

はみ出したままとする（結果を確認し，行の分割等の措置を行うのは問題作成者の責任とする）。

プログラムは警告を出力するものとする。

## 問題記述言語

問題を記述するために，簡易な記述言語を規定する。

外見は front matter つきの markdown 形式とする。

1ページ分の問題に対して1ファイルとする。

1回の改行は前後を繋げる(markdownと同様)。空行は段落がわかれる。
行末スペース2つによる強制改行は利用可能とする。

### front matter (タイトル，問題説明エリア)

```markdown
---
title: "ひらカナマスター"
number: "0001"
desc: "え や ぶんしょう と あうように わくのなかに じをかいてください。"
---
```

- title には，タイトル の文字列を記載する。省略可。
- number には，問題種別番号を算用数字4桁で記載する(ただし front matter には "0001" のように文字列として記載する)。
    - 省略した場合は，ファイル名の nnnn 部分が使用される。例えば "0001.md" や"0001_description.md" であれば "0001" が使用される。
- desc には，問題の説明文を記載する。省略可。


### 内容エリア

#### 開始

```markdown
# Content
```

内容エリア開始を示すのは，第1レベルタイトルで "Content" (case insensitive) とする。

#### セクションタイトル

```markdown
## ももたろう
```

セクションタイトルは，第2レベルタイトルを使用する。

#### 地の文

```markdown
ここに地の文を記載する。

空行が入った場合は改行する。
```

地の文は，markdown で通常の行として記載する。

#### 記入欄

```markdown
地の文と混在で{}{}{}記入欄を作ることができる
```

記入欄(1文字分)を `{}` で表す。`{}{}{}` であれば連続3文字分の記入欄となる。

欄に付加情報を付けることができる。

薄い灰色の文字で欄内にヒントを入れる場合は `{char: "ア"}` のように char キーを指定する。この文字は，記入欄内の上下左右中心を文字のemボックスの中心として，薄い灰色で 24mm × 24mm の大きさで配置される。文字のemボックスは24mmとする。このとき，記入欄内の点線の補助線は描画しない。

ふりがなを打つ場合は `{ruby: "あ"}` のように ruby キーを指定する。この文字は，記入欄の右側，上下中央に 9pt で記載される。記入欄とふりがなは 3pt の隙間を空ける。

ヒントとふりがなは同時に指定することができる。`{char: "ア", ruby: "あ"}` のように記載することができる。

ヒント文字，ふりがな，ともにvert/vrt2とJIS90字形を適用する。

記入欄の文法を使って，ダミーのスペースを作成することができる。`{space: 幅}` のように space キーを指定し，幅を mm 単位で記載すると，枠線や点線，文字は描画されず，指定幅の縦スペースになる。

char / ruby / space 以外のキーは無視される。

各キーの値は，引用符なしでの指定を可とする。

`{` `}` 文字自体を使用することは本プロジェクトでは想定されないので，`{` `}` 文字に対するエスケープは定義しない。

#### 画像

```markdown
![](画像ファイルパス)
```

画像は，正方形のエリアで 横30mm × 縦30mm の大きさとし，枠はない。指定された画像を縮小して表示する。代替テキストは指定できない(書かれても無視する)。

画像は正方形の画像を指定することが原則であるが，仮に長方形の画像が指定された場合は，長辺側が30mmになるように縮小する(画像の占有エリア自体は 30mm × 30mm のまま)。

PDF に埋め込む画像は，30mm × 30mm で 600dpi 相当(長辺 709px)に縮小し，印刷品質に大きな影響のない不可逆圧縮(JPEG，品質 90，クロマサブサンプリングなし)にする。オリジナルは img/ に保存したままとし，変換結果は tmp/imgcache/ にキャッシュする(元画像が更新された場合は再変換する)。元画像が既に長辺 709px 以下の JPEG(透過・回転なし)の場合は再エンコードせずそのまま用いる。透過画像は白背景に合成する。変換の実行/非実行は CLI オプション(--no-img-convert)で制御できる。変換には ruby-vips(gem)+ libvips を優先して使用し，無ければ Python 3 + Pillow を使用する。いずれも使用できない場合は警告のうえ元画像をそのまま埋め込む。

画像と地の文の間に空行を入れない場合は，画像は地の文の間にインライン表示される。
空行が入った場合は，画像は単一の行として配置される。

```markdown
このように
![](画像ファイルパス)
{}{}{}
同一行で配置できる。
```

この例では，「このように」という地の文，画像，連続3文字の記入欄，「同一行で配置できる。」という地の文が縦1行に配置される。

### こっそり解答エリア

```markdown
# Answer
```

こっそり解答エリア開始を示すのは，第1レベルタイトルで "Answer" (case insensitive) とする。

#### 解答

記入欄に書かれるべき解答を，箇条書きで記載する。

箇条書きは，こっそり解答エリアに1行にして記載される。

```markdown
- いぬ
- さる
- きじ
```

この例では，こっそり解答エリアに「こたえ：いぬ　さる　きじ」と展開される。箇条書きに書かれた文言が，全角スペースを間に挟んで連結される。

### 仕様外のmarkdown文法の扱い

3レベル以上のタイトル行(`### Title`等)，強調，`# Content` よりも前の本文，`# Answer` 以外での箇条書きは，仕様外として無視される。ただし，書き間違いを指摘するためにプログラムより警告を出力する。

## プロンプト(参考)

絵を作成する際の参考プロンプトであり，他の処理には使用しない。

- ChatGPT

```
未就学児(4～6歳想定)向けに，ある言葉を想起させる絵に対して，言葉を ひらがな または カタカナ でマス目に1文字ずつ書くスタイルの，ひらがな／カタカナの練習問題を作成する。 以下の言葉について，対象者に理解しやすく別の単語に誤解されにくい絵を1254x1254pxのPNG形式で生成してください。複数の言葉が指定されている場合は，1つの言葉に対して1つの画像ファイルになるように生成してください。
絵のテイストは添付画像に合わせてください。
```
